# Define your variables:
name = "John"
location = "New York"

# Read the text file:
with open('template.txt', 'r') as file:
    content = file.read()

# Use the `format()` method to replace the placeholders:
formatted_content = content.format(name, location)

print(formatted_content)
